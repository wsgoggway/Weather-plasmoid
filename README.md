# Open-Meteo Weather — KDE Plasma 6 Plasmoid

Виджет погоды для KDE Plasma 6, использующий **бесплатный** API [Open-Meteo](https://open-meteo.com).

> **Не требует API-ключа.** Просто установите и пользуйтесь.

## Возможности

- 🌡 Текущая температура, «ощущается как» (с **язвительной шуткой**), влажность, давление, облачность
- 💨 Скорость и направление ветра (русские румбы: С, СВ, ЮЗ…)
- 🌅 Восход и закат, ☀️ УФ-индекс, 🌧 осадки за текущий день
- 📅 Прогноз до 16 дней (по дням / по часам / оба режима): макс./мин. температура, вероятность осадков
- 📌 Компактный вид для панели (эмодзи + температура)
- 🖥 Развёрнутый вид: карточки текущей погоды, метрик дня и прогноза
- 🎉 Праздники сегодня (htmlweb.ru раз в сутки + оффлайн-фолбэк) и 📜 «этот день в истории» (Wikipedia, ru)
- 🔄 Автообновление (настраиваемый интервал)
- 🇷🇺 Русские названия погодных условий и направлений ветра
- 🌍 Настраиваемые единицы измерения (°C/°F, m/s/kmh/mph/kn)
- 🕐 Поддержка часовых поясов
- 🎨 Вписывается в тему Plasma (Kirigami)

## Требования

- **KDE Plasma 6** (или Plasma 5 с `metadata.desktop`)
- Подключение к интернету
- **Бесплатно** — API-ключ не требуется!

## Установка

```bash
# Для текущего пользователя
./install.sh --user

# Системная установка (требует sudo)
./install.sh --system
```

Перезапустите plasmashell:

```bash
plasmashell --replace &
```

## Настройка

1. Добавьте виджет: ПКМ по панели → **Add Widgets** → **Environment & Weather** → **Open-Meteo Weather**
2. ПКМ по виджету → **Configure...**:
   - **City name** — название города (для отображения)
   - **Latitude / Longitude** — координаты (Москва: 55.75, 37.62)
   - **Timezone** — часовой пояс (Europe/Moscow, Asia/Yekaterinburg, ...)
   - **Update interval** — интервал обновления (5–180 минут)
   - **Temperature unit** — °C или °F
   - **Wind speed unit** — m/s, km/h, mph, knots
   - **Forecast mode** — по дням / по часам / оба
   - **Forecast days** — до 16 дней
   - **Сегодня** — показывать праздники и «этот день в истории» (вкл/выкл)

## Структура проекта

```
kde_ya_wether/
├── README.md
├── AGENT.md                         # Правила для контрибьюторов/агента
├── install.sh                       # Установочный скрипт
└── package/
    ├── metadata.json                # Метаданные Plasma 6 (Id, Version, KPlugin)
    └── contents/
        ├── ui/
        │   ├── main.qml             # Корень: состояние, сеть, парсинг, шутки
        │   ├── CompactRepresentation.qml   # Компактный вид (панель)
        │   ├── FullRepresentation.qml      # Развёрнутый popup (карточная вёрстка)
        │   ├── ForecastItem.qml      # Карточка прогноза (daily/hourly)
        │   ├── DetailRow.qml         # Строка детализации (icon + label + value)
        │   └── configGeneral.qml     # Страница настроек (KCM.SimpleKCM)
        └── config/
            ├── config.qml            # ConfigModel → категория «Основные»
            └── main.xml              # Схема KConfig XT (типы + дефолты)
```

## Как это работает

Чистый **QML + JavaScript** — без Python, C++ или внешних зависимостей:

1. `main.qml` содержит таймер, HTTP-запросы к `api.open-meteo.com/v1/forecast` через `XMLHttpRequest`, парсинг JSON
2. Запрашиваются `current` (текущая погода), `daily` (метрики дня + прогноз) и `hourly` (почасовой) переменные
3. WMO-коды погоды (0–99) преобразуются в текст и эмодзи
4. Давление конвертируется из гПа в мм рт. ст.
5. Направление ветра из градусов — в русские обозначения (С, СВ, ЮЗ, ...)
6. Температуры округляются; «ощущается как» сопровождается язвительной шуткой по диапазону
7. Параллельно подгружаются праздники (оффлайн) и «этот день в истории» (Wikipedia, ru)
8. UI-компоненты автоматически обновляются через QML data binding

> Правила разработки (соглашения, workflow, антипаттерны) — в [`AGENT.md`](./AGENT.md).

## Источники данных

- **Погода и геокодинг городов:** [Open-Meteo](https://open-meteo.com) (без ключа, CC BY 4.0)
- **Геолокация по IP:** ip-api.com (фолбэк — ipwhois.app)
- **Этот день в истории:** Wikipedia REST API (ru)
- **Праздники:** [htmlweb.ru](https://htmlweb.ru/service/holidays.php) (раз в сутки, без ключа) с фолбэком на встроенный оффлайн-словарь

## Open-Meteo API

- **Эндпоинт:** `https://api.open-meteo.com/v1/forecast`
- **Бесплатно** для некоммерческого использования
- До **10 000 запросов/день** бесплатно
- Модели: GFS, ECMWF, MeteoFrance, JMA, ...
- [Документация](https://open-meteo.com/en/docs)

## Лицензия

MIT. Данные погоды: [open-meteo.com](https://open-meteo.com) (CC BY 4.0)
